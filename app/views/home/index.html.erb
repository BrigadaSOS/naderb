<div class="mx-auto my-auto text-center space-y-8">
  <div>
    <h1 class="text-4xl font-bold text-foreground mb-2"><%= t("home.welcome_title") %></h1>
    <p class="text-muted-foreground"><%= t("home.welcome_subtitle") %></p>
  </div>

  <%= discord_button(
    t("home.login_with_discord"),
    user_discord_omniauth_authorize_path,
  ) %>
</div>
