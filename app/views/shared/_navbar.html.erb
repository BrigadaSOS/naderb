<nav class="bg-primary shadow-lg" data-turbo-permanent>
  <div class="px-3 sm:px-6">
    <div class="flex justify-between items-center h-16">
      <!-- Left side: Hamburger (mobile only) + Logo -->
      <div class="flex items-center space-x-2 sm:space-x-3">
        <!-- Hamburger Button (visible only on mobile when on dashboard pages) -->
        <% if user_signed_in? && request.path.start_with?('/dashboard') %>
          <label for="mobile-menu-toggle" class="lg:hidden cursor-pointer text-primary-foreground hover:text-primary-foreground/80 transition-colors p-1">
            <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
            </svg>
          </label>
        <% end %>
        <%= link_to root_path, class: "flex items-center space-x-2 sm:space-x-3 text-primary-foreground font-bold text-sm sm:text-xl hover:text-primary-foreground/80 transition-colors duration-200" do %>
          <%= image_tag "icon.png", alt: "Brigada SOS", class: "w-6 h-6 sm:w-8 sm:h-8" %>
          <span>Brigada SOS Japonés</span>
        <% end %>
      </div>
      <!-- Right side: Discord Link -->
      <div class="flex items-center space-x-2 sm:space-x-4">
        <!-- Discord Link -->
        <a
          href="https://discord.gg/ajWm26ADEj"
          class="text-primary-foreground/80 hover:text-primary-foreground transition-colors duration-200"
          target="_blank"
          rel="noopener noreferrer"
        >
          <%= discord_icon("w-5 h-5 sm:w-6 sm:h-6") %>
        </a>
      </div>
    </div>
  </div>
</nav>
