<div class="flex h-full w-full">
  <%= render "dashboard/sidebar" %>

  <div class="flex-1 p-6 overflow-y-auto">
    <div class="mb-8">
      <h1 class="text-3xl font-bold text-foreground mb-2">Edit Message</h1>
      <p class="text-muted-foreground">Update "<%= @scheduled_message.name %>"</p>
    </div>

    <%= render "form" %>
  </div>
</div>
